# Roadmap Archive: v1.7 Integration & Polish

**Status:** ✅ Complete (1 documented deferral)
**Shipped:** 2026-05-25
**Phases:** 2 (23, 24)
**Requirements:** 5/6 satisfied (WIRE-01 deferred — blocked on dm20 event surface)
**Plans completed:** 4
**Audit:** [.planning/v1.7-MILESTONE-AUDIT.md](../v1.7-MILESTONE-AUDIT.md) — status: passed

## Core Goal

Close v1.6's honest-gap follow-ups (cog-wiring + AOE addendum), ship
cross-platform CI matrix, bundle Phoenix dashboards for v1.5 caches,
resolve atomicity wording reconciliation.

## Key Accomplishments

1. **`/end_game` slash command (WIRE-02)** — lobby cog gains the command; calls `MonsterMemoryRegistry.purge_session` on success; ephemeral confirmation embed.
2. **AOE addendum live integration (WIRE-03)** — SmartMonsterDriver conditionally injects Phase 20's versioned addendum when `available_actions` contains ≥2 AOE-kind entries. `eldritch.aoe.addendum_version` OTel attribute on outer decision span.
3. **WIRE-01 honest deferral** — first Phase 23 agent halted with 188-line halt-report identifying dm20's lack of structured damage-event surface; user accepted Option B (partial); second agent shipped WIRE-02 + WIRE-03 cleanly with full disclosure.
4. **Cross-platform CI matrix (POLISH-01)** — `.github/workflows/ci.yml` runs macos-latest + ubuntu-latest × Python 3.11. Default omits `[mac-ocr]`/`[observability]` (tests off-path). Optional `extras-mac` job tests on-path (continue-on-error). Linux runner verifies Phase 14 skip-gates work cleanly.
5. **3 bundled cache dashboards (POLISH-02)** — `mcp_cache.json`, `character_cache.json`, `narrcache.json` in `database/dashboards/`; OUR-FORMAT spec; seed-dashboards.sh extended.
6. **Doc-fixes (POLISH-03)** — atomicity wording corrected in `milestones/v1.6-REQUIREMENTS.md` (agent caught my reference error — wording lived in v1.6 OPQOL-03, not v1.5). `UPSTREAM-ISSUES.md` created with gsd-tools planner-template gap as ISSUE-1.
7. **Mid-execution discovery (Phase 24 → CI gate)** — found 14 SUMMARYs (Phases 16-22) missing `requirements_completed:` frontmatter; backfilled inline post-merge. CI gate now reports `OK: 35 SUMMARY files`.
8. **Full suite GREEN** — 1644 passed, 17 skipped, 0 failed.

## Phase Details

### Phase 23: Honest-gap closure (cog-wiring + AOE prompt integration)
**Goal**: Wire MonsterMemory observe_hit + session-close hook into bot cogs (close v1.6 Phase 21 honest gap). Integrate AOE addendum into SmartMonsterDriver's live oracle prompt assembly (close v1.6 Phase 20 ship-but-not-wired gap).
**Requirements**: WIRE-01, WIRE-02, WIRE-03
**Plans**:
- [ ] Plan 01: MonsterMemory cog-wiring + /end_game hook (`feat(23-01): observe_hit cog wiring + session-close purge`)
- [ ] Plan 02: AOE addendum live prompt assembly (`feat(23-02): conditional AOE addendum injection`)

### Phase 24: CI matrix + Phoenix dashboards + doc-fix
**Goal**: Linux CI runner (verify Phase 14 skip-gates work); bundled Phoenix dashboards for the 3 caches (MCP/character/narration); atomicity wording doc-fix + gsd-tools upstream-issues backlog file.
**Requirements**: POLISH-01, POLISH-02, POLISH-03
**Plans**:
- [ ] Plan 01: GitHub Actions matrix (`feat(24-01): cross-platform CI matrix macos+ubuntu`)
- [ ] Plan 02: Cache dashboards + doc fixes (`docs(24-02): cache dashboards + atomicity doc-fix + UPSTREAM-ISSUES.md`)

## Closing Notes

v1.7 was the integration-and-polish milestone — close gaps, set up the
infrastructure for cross-platform development, and clean up doc-debt.
The Phase 23 lifecycle (halt → user-override → Option-B ship → full
disclosure) is the canonical example of the honest-report contract
preventing premature shipping while preserving velocity.

WIRE-01 stays open as a v1.8 candidate IF dm20 ships structured
damage-event surface. Until then, MonsterMemory continues to work at
the driver level (the persistence layer is complete; only the cog-side
observation wiring is blocked).

---
*Archived 2026-05-25 by `/gsd-complete-milestone v1.7`*
