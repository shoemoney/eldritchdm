# Roadmap Archive: v1.3 Hygiene Sweep

**Status:** ⚠️ Complete with accepted partial (tech_debt-disposition)
**Shipped:** 2026-05-25
**Phases:** 1 (Phase 14)
**Requirements:** 2.5/3 satisfied (FLAKE-01 ✓, FLAKE-03 ✓, FLAKE-02 partial)
**Plans completed:** 2
**Audit:** [.planning/v1.3-MILESTONE-AUDIT.md](../v1.3-MILESTONE-AUDIT.md) — status: tech_debt (user-accepted)

## Core Goal

Close the carried-since-v1.1 test flakes (OCR backend env failures +
phase3_smoke test-pollution flake) and bring SUMMARY.md frontmatter into
spec compliance. Single focused phase of low-risk hygiene.

## Key Accomplishments

1. **OCR + prometheus_client skip-gates (FLAKE-01)** — `pip install -e ".[dev]"` no longer needs the `[mac-ocr]` extras for a clean suite run. Tests properly skip-marked with clear messages.
2. **SUMMARY.md frontmatter backfilled (FLAKE-03)** — all 14 v1.1+v1.2 SUMMARYs now have `requirements_completed:` YAML field. Backfill script (`scripts/audit/backfill_summary_frontmatter.py`) + CI gate (`scripts/ci/check_summary_frontmatter.sh`) committed.
3. **75% reduction in test failures** — 8 failed → 2 failed (targeted suite). The 2 remaining failures are newly-surfaced pre-existing pytest HANGS, not v1.3 regressions.
4. **WRITER-QUEUE-HANG-01 surfaced** — Phase 14's logging-polluter fix revealed two pre-existing pytest hangs in `test_writer_queue_drain_timeout` and `test_close_cleanly_shuts_down`. Both predate v1.3 (Phase 5 + Phase 6 origin); cannot be killed by `pytest-timeout` (C-level thread boundary). Properly scoped as v1.4 work.
5. **Honest reporting honored** — executor halted-and-reported on FLAKE-02 per the HARD CONSTRAINT in the brief, rather than silently marking complete. The autonomous-mode contract worked as designed.

## Phase Details

### Phase 14: Flake cleanup + planner template hardening
**Goal**: Close the carried-since-v1.1 test flakes (OCR backend env failures + phase3_smoke test-pollution flake) and backfill `requirements_completed:` frontmatter across all v1.1+v1.2 SUMMARYs. Sets up a green-on-green test suite for v1.4 feature work.
**Mode:** mvp (hygiene — no user-visible behavior change)
**Depends on**: Phase 13 (v1.2 final state — full corpus to audit)
**Requirements**: FLAKE-01, FLAKE-02, FLAKE-03
**Success Criteria**:
  1. Full `uv run pytest tests/` returns 0 ocrmac-related failures (skip-gated cleanly OR ocrmac installed in dev venv)
  2. `tests/integration/test_phase3_smoke.py` passes deterministically in full-suite run; root-cause polluter identified and fixed at source
  3. All 14 v1.1+v1.2 SUMMARY.md files have `requirements_completed:` YAML frontmatter field listing the REQ-IDs each plan satisfied
  4. ruff + lint-imports clean; no new test failures introduced
**Plans**:
- [ ] Plan 01: OCR env-gate + phase3_smoke pollution root-cause + fix (`fix(14-01): OCR env-gate + phase3_smoke pollution root-cause fix (FLAKE-01, FLAKE-02)`)
- [ ] Plan 02: SUMMARY frontmatter backfill across v1.1+v1.2 phases (`docs(14-02): backfill requirements_completed: frontmatter across all v1.1+v1.2 SUMMARYs (FLAKE-03)`)

## Closing Notes

This is exactly the "you fix one layer of debt, the next layer becomes
visible" outcome that hygiene phases exist for. FLAKE-01 + FLAKE-03 are
fully closed; FLAKE-02 is partially closed at the layer Phase 14 owned
(structlog/stdlib logging polluter), with the residual issue properly
scoped to WRITER-QUEUE-HANG-01 in v1.4.

The accepted-partial disposition (status: tech_debt) is documented in
the audit — operators reading this archive can see exactly what was
shipped vs. deferred.

---
*Archived 2026-05-25 by `/gsd-complete-milestone v1.3`*
