# Roadmap Archive: v1.4 Writer-Queue Reliability

**Status:** ✅ Complete
**Shipped:** 2026-05-25
**Phases:** 1 (Phase 15)
**Requirements:** 3/3 satisfied
**Plans completed:** 1
**Audit:** [.planning/v1.4-MILESTONE-AUDIT.md](../v1.4-MILESTONE-AUDIT.md) — status: passed

## Core Goal

Fix the residual FLAKE-02 (v1.3 carried-partial) and verify the
"WRITER-QUEUE-HANG-01" diagnosis v1.3 surfaced. Outcome: hangs don't
reproduce (resolved upstream by v1.3's logging-polluter fix); the real
remaining bug was a test-isolation issue in `bot_factory` — fixed via
snapshot+restore of `sys.modules[cog]` in a suite-wide autouse fixture.

## Lifecycle Highlights (the honest-report contract in action)

1. **First Phase 15 agent halted on premise check.** Empirical
   reproduction showed v1.3's "writer-queue hangs" don't reproduce at
   HEAD, and the `WriterQueue` already implements the fix v1.4 CONTEXT
   was about to propose. Halted with 169-line evidence-rich halt-report
   identifying the REAL bug (FLAKE-02 test-isolation in `bot_factory`).
2. **Orchestrator tried halt-report option (c) inline** (sys.modules
   autouse-clear). Pytest hung in 3 verification runs. Reverted untested;
   marked v1.4 halt-deferred.
3. **User overrode the halt.** Spawned a fresh agent with rescoped
   FLAKE-02 scope. Worktree isolation immune to orchestrator's hang quirk.
4. **Second agent shipped the actual fix** — and corrected one of the
   halt-report's hypotheses: `unload_extension` in teardown alone is
   insufficient. Full fix is snapshot+restore so `mock.patch` lands on
   the same module dict the test class captured at collection.

End result: 1244 passed, 17 skipped, 0 failed in 2 consecutive full-suite
runs. First clean green since the v1.1→v1.2 cycle.

## Key Accomplishments

1. **FLAKE-02 closed** — test-isolation fix via `tests/conftest.py` autouse fixture (snapshot+restore of `sys.modules[cog]` around every test) + `tests/bot/conftest.py` extension-unload teardown for defense in depth.
2. **HANG-01 + HANG-02 verified not reproducible** — v1.3's writer-queue-hang diagnosis was an artifact; resolved upstream by v1.3 Phase 14's logging-polluter fix.
3. **Halt-report preserved** as artifact at `.planning/phases/15-writer-queue-fix/15-HALT-REPORT.md` (169 lines, full bisect evidence). Demonstrates the autonomous-mode honest-report contract working under stress.
4. **First full-suite GREEN since v1.1** — 1244 passed, 0 failed, 2 consecutive runs. The "orthogonal failures" framing carried for 3 milestones is retired.
5. **v1.3 partial closed** — `.planning/milestones/v1.3-REQUIREMENTS.md` FLAKE-02 back-ticked [x] with v1.4 cross-reference.

## Phase Details

### Phase 15: Writer-queue shutdown rewrite + FLAKE-02 closure (rescoped)
**Original goal**: Fix WRITER-QUEUE-HANG-01 (v1.3 surfacing) — proven incorrect by reproduction.
**Final goal**: Close FLAKE-02 via test-isolation fix.
**Mode:** mvp (test reliability)
**Depends on**: Phase 14 (v1.3 partial state)
**Requirements**: HANG-01, HANG-02, HANG-03
**Final fix**: `tests/conftest.py` autouse fixture (snapshot/restore of `sys.modules` cog entries) + `tests/bot/conftest.py` `bot_factory` async-yielding with `unload_extension` teardown.
**Plans**: 1 ship plan (15-01) + 1 halt artifact (15-HALT-REPORT)

## Closing Notes

This milestone is the cleanest demonstration of GSD's honest-report
contract in this project's history. First agent had every incentive to
silently produce a fix; instead it halted with evidence when premises
broke. Second agent built on that evidence and shipped the actual fix.
Audit caught and documented one final detail (snapshot+restore vs. pure
unload). v1.0→v1.4 production-ready.

---
*Archived 2026-05-25 by `/gsd-complete-milestone v1.4`*
