---
phase: 30-install-troubleshooting
plan: 30-01
type: standard
autonomous: true
requirements: [DOCS-01]
---

# Plan 30-01 — INSTALL.md refresh (Docker quickstart, env vars, CLIs, dep groups, cross-links)

## Objective
Refresh `INSTALL.md` to reflect v1.0→v1.9 surface that has accumulated since the original install guide was written: add a Docker quickstart (Phase 29 artifacts), document the missing env vars (those actually consumed by `Settings` or `os.environ` in src/), document the seven `[project.scripts]` CLIs, list the four `[project.optional-dependencies]` groups, and replace the inline mini-troubleshooting and v1.0→v1.1 upgrade sections with cross-links to the new `docs/TROUBLESHOOTING.md` and `docs/UPGRADE.md` produced in 30-02.

## Context
@.planning/phases/30-install-troubleshooting/30-CONTEXT.md (D-229..D-234)

## Hard constraints (D-232)

Every claim cross-references existing source. Authoritative sources:
- env vars in `Settings`: `src/eldritch_dm/config/__init__.py` (the Settings class)
- env vars NOT in `Settings` but read via `os.environ`:
  - `OBSERVABILITY_ENABLED` → `src/eldritch_dm/observability/tracer.py:39`
  - `OBSERVABILITY_METRICS_ENDPOINT` → `src/eldritch_dm/observability/metrics_endpoint.py:51`
  - `ELDRITCH_DAILY_LLM_BUDGET_USD` → `src/eldritch_dm/tools/cost_report.py:84`
  - `DM20_MCP_URL` → `src/eldritch_dm/tools/backfill_pc_classes.py`
- CLIs: `[project.scripts]` in `pyproject.toml`
- Dep groups: `[project.optional-dependencies]` in `pyproject.toml`
- Docker quickstart: `docker-compose.yml`, `Dockerfile`, `.dockerignore` from Phase 29

## Tasks

<task id="1" type="auto">
  <what>Add a Docker quickstart section to INSTALL.md (after the Quick path / before Component 1)</what>
  <files>INSTALL.md</files>
  <behavior>
    Insert a new `## 🐳 Docker quickstart (one command)` section after the existing `## ⚡ Quick path` section (line ~109) and before `## 🧠 Deep install — Component 1`. Content:
      - Single-command bring-up:
        ```
        cp .env.example .env  # then edit DISCORD_TOKEN + (if oMLX on host) MLX_BASE_URL
        docker compose up -d
        docker compose logs -f eldritch-bot
        ```
      - Explain that the container is the bot only — `oMLX` and `dm20 MCP` still run on the host (Apple Silicon required). Container reaches them via `host.docker.internal:8765` on Docker Desktop; Linux gets parity through the compose file's `extra_hosts: host-gateway` mapping (citation: `docker-compose.yml` lines 49-52).
      - Optional observability stack: `docker compose -f docker-compose.observability.yml up -d` (Phase 11 / OBS-01, existing).
      - Cross-link: "Hit an issue? See [docs/TROUBLESHOOTING.md](docs/TROUBLESHOOTING.md). Coming from an older release? See [docs/UPGRADE.md](docs/UPGRADE.md)."
  </behavior>
</task>

<task id="2" type="auto">
  <what>Refresh the env-var reference table in INSTALL.md with the 12 D-229 vars</what>
  <files>INSTALL.md</files>
  <behavior>
    Find the existing env-var table (in or near `## 🔑 Configure .env`, around line 442). Add a new sub-section `### 📋 Env var reference (v1.10)` that lists the 12 vars from D-229 with their authoritative source path and a one-line purpose. The 12 vars (from D-229; cross-checked against Settings + observability + tools):

    | Variable | Default | Source | Purpose |
    |---|---|---|---|
    | `DISCORD_TOKEN` | (unset; required) | `Settings.discord_token` | Discord bot token. Bot exits **4** if missing (Phase 7 SAFETY-03). |
    | `DISCORD_GUILD_IDS` | `""` | `Settings.discord_guild_ids` | CSV of guild IDs for instant slash-command sync. Empty → global. |
    | `DISCORD_OWNER_ID` | `None` | `Settings.discord_owner_id` (alias `DISCORD_OWNER_ID`) | Owner Discord user ID; receives budget + degraded-mode DMs (Phase 22 / OPQOL-02 / D-170). |
    | `ELDRITCH_DB_PATH` | `./eldritch.sqlite3` | `Settings.eldritch_db_path` | SQLite DB path (WAL journaling). |
    | `DM20_MCP_URL` | `OMLX_ENDPOINT` fallback | `src/eldritch_dm/tools/backfill_pc_classes.py` | Override for the dm20 MCP endpoint used by `eldritch-dm-backfill-pc-classes` (D-48). |
    | `OBSERVABILITY_ENABLED` | `false` | `src/eldritch_dm/observability/tracer.py:39` (`os.environ.get`) | When `true`, OTel tracing wires up; otherwise the observability tree is a lazy no-op (Phase 11 / OBS-01). |
    | `MONSTER_DRIVER` | `smart` | `Settings.monster_driver` (alias `MONSTER_DRIVER`) | `smart` / `random` / `mixed`. `random` is the v1.0 escape hatch (Phase 10 / D-52). |
    | `NARRCACHE_ENABLED` | `false` | `Settings.narrcache_enabled` (alias `NARRCACHE_ENABLED`) | Opt-in narration cache; off by default (Phase 18 / D-129 — mechanical-honesty contract). |
    | `MCPCACHE_L2_ENABLED` | `false` | `Settings.mcpcache_l2_enabled` (alias `MCPCACHE_L2_ENABLED`) | Opt-in L2 SQLite cache. L1 is on by default. (Phase 16 / D-117.) |
    | `MONSTER_MEMORY_PERSIST` | `false` | `Settings.monster_memory_persist` (alias `MONSTER_MEMORY_PERSIST`) | Opt-in cross-restart monster memory (Phase 21 / D-160). |
    | `ELDRITCH_DAILY_LLM_BUDGET_USD` | `5.00` | `src/eldritch_dm/tools/cost_report.py:84` | Daily LLM-spend ceiling used by `eldritch-dm-cost-report` (Phase 13 / MON-03). |
    | `STREAM_ENABLED` | `true` | `Settings.stream_enabled` (alias `STREAM_ENABLED`) | When `true`, SmartMonsterDriver emits the "🤔 sizing up the party…" indicator (Phase 19 / STREAM-03). Set `false` for v1.5 silent behavior. |

    Add a footnote: "Full `.env` surface (including ingest-backend selection, cache TTLs, log routing) is documented in [.env.example](.env.example). The table above is the new-since-v1.0 surface."
  </behavior>
</task>

<task id="3" type="auto">
  <what>Add CLIs reference section to INSTALL.md (7 entries from [project.scripts])</what>
  <files>INSTALL.md</files>
  <behavior>
    After the env-var reference (task 2), add `### 🛠️ Console scripts (after `pip install -e .`)`. Pull the 7 entries directly from `pyproject.toml` `[project.scripts]`:

    | Command | Module | Use case |
    |---|---|---|
    | `eldritch-dm` | `eldritch_dm.bot.__main__:main` | The bot entry point. Equivalent to `python -m eldritch_dm.bot`. |
    | `eldritch-dm-backfill-pc-classes` | `eldritch_dm.tools.backfill_pc_classes:main` | v1.0→v1.1 upgrade: populates `pc_classes` for existing characters so Riposte fires (Phase 9 / TD-3). |
    | `eldritch-dm-eval` | `eldritch_dm.eval.cli:main` | LLM-as-judge tactical scoring runner (Phase 12 / EVAL-03). |
    | `eldritch-dm-cost-report` | `eldritch_dm.tools.cost_report:main` | Daily LLM-spend report from the local span buffer (Phase 13 / MON-03). Respects `ELDRITCH_DAILY_LLM_BUDGET_USD`. |
    | `eldritch-dm-cache-clear` | `eldritch_dm.tools.cache_clear:main` | Operator cache-purge for the character snapshot cache (Phase 17 / CHARCACHE-03). |
    | `eldritch-dm-cache-disable` | `eldritch_dm.tools.cache_disable:main` | Operator runtime disable/enable for narration cache (Phase 18 / NARRCACHE-03). |
    | `eldritch-dm-cache-stats` | `eldritch_dm.tools.cache_stats:main` | Print narration cache hit/miss + size (Phase 18 / NARRCACHE-03). |
    | `eldritch-dm-perf-baseline` | `eldritch_dm.tools.perf_baseline:main` | Run hot-path profiler + diff against committed baseline (Phase 28 / TUNE-02 / D-218). Exit codes 0/1/2. |

    (8 rows — the table includes `eldritch-dm` itself for completeness; the 7 tools from D-229 are rows 2-8.)
  </behavior>
</task>

<task id="4" type="auto">
  <what>Add optional dependency groups section to INSTALL.md</what>
  <files>INSTALL.md</files>
  <behavior>
    After the CLIs table (task 3), add `### 📦 Optional dependency groups`. From `pyproject.toml` `[project.optional-dependencies]`:

    | Group | Install | Contents | When to install |
    |---|---|---|---|
    | `dev` | `pip install -e ".[dev]"` | pytest, pytest-asyncio, pytest-cov, pytest-mock, ruff, respx, import-linter, syrupy, reportlab | Contributing or running the test suite. |
    | `mac-ocr` | `pip install -e ".[mac-ocr]"` | `ocrmac>=1.0,<2.0` (Apple Vision via PyObjC) | macOS 10.15+ — primary OCR for character-sheet ingest. |
    | `linux-ocr` | `pip install -e ".[linux-ocr]"` | `easyocr>=1.7,<2.0` | Linux / cross-platform OCR fallback (Phase 14 / FLAKE-01 skip-gate auto-skips OCR tests when neither is installed). |
    | `observability` | `pip install -e ".[observability]"` | opentelemetry-api/sdk/exporter-otlp-proto-http, prometheus_client | Required to actually emit spans/metrics when `OBSERVABILITY_ENABLED=true` (Phase 11 / OBS-01; Phase 13 / MON-01). |

    Mention: extras can be combined, e.g. `pip install -e ".[dev,mac-ocr,observability]"`.
  </behavior>
</task>

<task id="5" type="auto">
  <what>Replace inline Troubleshooting + v1.0→v1.1 Upgrade sections with cross-links (D-233)</what>
  <files>INSTALL.md</files>
  <behavior>
    The existing `## 🩺 Troubleshooting` (line 722) and `## v1.0 → v1.1 Upgrade: pc_classes Backfill` (line 919) sections duplicate what 30-02 produces. Per D-233 they should redirect, not fork.

    - Replace the body of `## 🩺 Troubleshooting` (722-820) with: a one-line summary + cross-link to `docs/TROUBLESHOOTING.md`, retaining the section heading as a discoverability anchor. List the 4 most-common entries inline as bullets with anchors to the new file (e.g. "DM is offline → [#bot-says-dm-is-offline](docs/TROUBLESHOOTING.md#bot-says-dm-is-offline)").
    - Replace the body of `## v1.0 → v1.1 Upgrade` (919-end of section) with a one-paragraph summary and a cross-link to `docs/UPGRADE.md#v10--v11`. The detailed flags / exit-codes / re-running notes live in UPGRADE.md.
    - Do NOT remove the section headings — they keep existing anchor links in third-party docs alive.
  </behavior>
</task>

<task id="6" type="auto">
  <what>Add top-of-doc cross-link banner near the Hero section (D-233)</what>
  <files>INSTALL.md</files>
  <behavior>
    Immediately after the `## 🎯 Hero` block (around line 38, before `## 🛠️ Prerequisites`), insert a short admonition block:

    > 🔗 **Other docs in this set:**
    > - [`docs/TROUBLESHOOTING.md`](docs/TROUBLESHOOTING.md) — FAQ for the most common operator-pain symptoms across v1.0-v1.10.
    > - [`docs/UPGRADE.md`](docs/UPGRADE.md) — version-to-version upgrade notes (v1.0 → v1.10).

    Single insert; ≤6 lines.
  </behavior>
</task>

## Verification

- INSTALL.md compiles (no broken markdown — verify with a header re-count using `grep -c "^## " INSTALL.md`)
- Every env var, CLI, and dep group cited in the new sections is grep-traceable to its source (pyproject / Settings / observability / tools)
- ruff is a no-op pass (no .py touched in this plan)
- `import-linter` is a no-op pass
- Existing 1680-test suite unaffected (smoke check: `pytest tests/ -x --co -q | tail -5` to confirm collection)
