---
phase: 31
plan: 01
type: auto
autonomous: true
wave: 1
depends_on: []
requirements: [SECAUDIT-01, SECAUDIT-02, SECAUDIT-03]
---

# Phase 31 Plan 01 — v1.11 Security Audit (read-only)

## Objective

Produce `.planning/SECURITY-AUDIT-v1.11.md` — a comprehensive read-only security
audit covering the 8 attack surfaces enumerated in SECAUDIT-01, with findings (if
any) categorized per the SECAUDIT-02 4-tier severity scheme and per-finding format
defined in D-238. Pure investigation — NO code changes (D-242).

Honesty clause (SECAUDIT-03 / D-239) is active: Branch B (0-finding) closure is a
legitimate outcome provided the methodology is itself audit-grade.

## Context

@.planning/phases/31-security-audit/31-CONTEXT.md
@.planning/REQUIREMENTS.md

Prior security work referenced (D-240):
- v1.0 Phase 1: SAN-01..06 (sanitizer + audit table)
- v1.1 Phase 7: SAFETY-01/02/03 (modal wiring, token guard)
- v1.4 Phase 15: persistence isolation
- v1.5 Phase 16-18: MCPCache / CharacterSnapshot / NarrCacheGate allow-lists
- v1.6 Phase 22: OPQOL-02 budget-owner Discord DM

## Tasks

<task id="1" type="auto">
<name>Investigate 8 attack surfaces and write the audit report</name>

<behavior>
Walk each of the 8 surfaces in SECAUDIT-01 order. For each surface, record:
1. Methodology — what grep patterns / files inspected
2. Findings (if any) per D-238 format: title, surface, repro (code-line cite),
   severity (D-237 tier), suggested fix (high-level only — Phase 32 owns
   remediation)
3. If no findings: "no findings + methodology" note with evidence trail.

Surfaces:
  1. Secret/token leak vectors
  2. Allow-list bypass paths
  3. Cache-poisoning vectors
  4. Sanitizer regression (3 modals)
  5. Mechanical-honesty contract
  6. Discord permission scope
  7. File-system path traversal
  8. Discord DM-to-owner content
</behavior>

<files>
.planning/SECURITY-AUDIT-v1.11.md
</files>

<done_when>
- File exists at the target path
- All 8 surfaces have a section
- Each surface section has at least one grep evidence line (auditable)
- Findings (if any) follow D-238 per-finding format
- Severity counts summarized in a header table
- Methodology section + tools-used disclosure included
- No `src/` or `tests/` `.py` file modified (verified by `git diff --stat src/ tests/`)
</done_when>
</task>

<task id="2" type="auto">
<name>Tick SECAUDIT-01/02/03 in REQUIREMENTS.md</name>

<behavior>
Flip `[ ]` to `[x]` for SECAUDIT-01, SECAUDIT-02, SECAUDIT-03 in
`.planning/REQUIREMENTS.md`. Append the phase to the traceability table row if
not already there.
</behavior>

<files>
.planning/REQUIREMENTS.md
</files>

<done_when>
- Three checkboxes flipped to `[x]`
- No other lines touched
</done_when>
</task>

## Success Criteria

- [ ] `.planning/SECURITY-AUDIT-v1.11.md` exists, all 8 surfaces covered
- [ ] No code changes (`git diff --stat src/ tests/` == 0)
- [ ] SECAUDIT-01/02/03 ticked
- [ ] Branch B (0 findings) is a valid closure — honesty clause permits it
