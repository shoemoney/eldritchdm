---
phase: 25-multi-channel-stress
plan: 25-02
type: standard
autonomous: true
wave: 2
depends_on: [25-01]
requirements: [CONC-03]
---

# Plan 25-02 — Fix-at-source OR honest-report (CONC-03)

## Objective

Close CONC-03 by either:

- **Branch A (fix-at-source):** if Plan 25-01's 4-channel stress test surfaces a
  concurrency bug that is fixable in <2 hours of investigation, fix it at the
  source. Add a regression test alongside.
- **Branch B (no-bug):** if 3 consecutive runs of Plan 25-01 pass with no
  surfaced bug, write a one-page no-op report documenting the baseline and
  closing CONC-03 honestly.
- **Branch C (architectural escalation):** if the surfaced bug requires
  architectural rework beyond v1.8 scope (>2 hour investigation), halt-and-
  report per D-198. Document the bug, minimal repro, and suggested fix path.
  Escalate to v1.9. Do NOT paper over.

The branch is selected AT EXECUTE TIME based on Plan 25-01's actual outcome —
not pre-committed here.

## Context

@.planning/phases/25-multi-channel-stress/25-CONTEXT.md (D-197, D-198, D-199)
@.planning/REQUIREMENTS.md (CONC-03)
@.planning/phases/25-multi-channel-stress/25-01-SUMMARY.md (post-Plan-01 outcome)

## Tasks

### Task 1 (auto) — Select branch based on Plan 25-01 outcome

**Implementation:**

Read `25-01-SUMMARY.md`. If the "3-run stress result" section shows all 3 runs
green AND no Known Stubs / Deferred Issues block describes a deferred concurrency
bug → **Branch B**. Otherwise → **Branch A** (if estimated fix <2hrs) or
**Branch C** (escalate).

### Task 2A (auto, Branch A only) — Fix surfaced bug at source

**Files:** depends on the bug. Likely candidates documented in 25-CONTEXT.md:
`src/eldritch_dm/mcp/cache.py` (L1 lock contention), `src/eldritch_dm/persistence/connection.py`
(WriterQueue), `src/eldritch_dm/gameplay/monster_memory.py` (registry races).

**Implementation:**
- Patch the offending module. Keep change minimal and surgical.
- Add a focused regression test under `tests/<area>/test_<bug>.py` that fails
  on the pre-fix code and passes after the fix.
- Re-run Plan 25-01's stress test 3× to confirm the fix.

### Task 2B (auto, Branch B only) — Write the no-op report

**Files:**
- `.planning/phases/25-multi-channel-stress/25-02-SUMMARY.md` (created by Step 3 anyway)

**Implementation:**
- State explicitly: "4-channel concurrency stress test ran 3 consecutive times
  with zero failures under RUN_STRESS=1. No bug surfaced. CONC-03 closed as
  no-op."
- Tag CONC-03 in REQUIREMENTS.md as `[x] complete` with rationale "no-op
  closure — baseline established by Plan 25-01."
- No source-code changes.

### Task 2C (auto, Branch C only) — Halt-and-report escalation

**Files:**
- `.planning/phases/25-multi-channel-stress/25-02-SUMMARY.md` (escalation document)

**Implementation:**
- Capture the bug: minimal repro, observed vs expected behavior, suspected root
  cause, files implicated, why fix exceeds v1.8 scope.
- Suggest a fix path (architectural sketch) for v1.9 planning to pick up.
- Leave CONC-03 unchecked in REQUIREMENTS.md with a `(deferred to v1.9 — see
  25-02-SUMMARY.md)` annotation.
- Do NOT modify the affected source files in this plan.

### Task 3 (auto) — Regression check + lint (all branches)

**Verification:**
```bash
cd /Users/shoemoney/Services/DiscordDM/.claude/worktrees/agent-af0d397254d23307f
.venv/bin/pytest -q
.venv/bin/ruff check src/ tests/ run.py
.venv/bin/lint-imports
# Re-run the stress test 3× if Branch A patched source:
for i in 1 2 3; do
  RUN_STRESS=1 .venv/bin/pytest tests/integration/test_multi_channel_stress.py -v
done
```

## Success Criteria

- One of the three branches taken with full documentation.
- If Branch A: regression test added; stress test still 3-for-3 green; ruff/
  lint-imports clean; full suite green.
- If Branch B: SUMMARY explicitly states no-op closure; CONC-03 ticked.
- If Branch C: SUMMARY captures bug + repro + suggested fix path; CONC-03 left
  with deferral annotation.

## Deviation Notes

Do NOT silently rebrand a Branch-C bug as Branch-B. Honesty contract per D-198.
