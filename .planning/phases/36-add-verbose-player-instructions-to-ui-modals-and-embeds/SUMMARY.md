# Phase 36: Add verbose player instructions to UI modals and embeds

## Outcomes
- Updated `DeclareActionModal` with clearer examples for action description.
- Updated `CharacterReviewModal` and `WeaponSelectModal` with verbose placeholders indicating exactly what format is expected.
- Updated `combat_embed` to state explicitly that only the active actor can use actions.
- Updated `lobby_embed` with hints to run `/upload_character_file` or `/upload_character_url` before clicking Ready.
- Updated `/start_game`, `/upload_character_url`, and `/upload_character_file` command descriptions for better onboarding.
- Updated Riposte reaction message to explain exactly *why* the Riposte is available and *what* the player should do.
