---
phase: 27-profiling
plan: 27-02
type: standard
autonomous: true
wave: 2
depends_on: [27-01]
requirements: [PROFILE-02]
generated: 2026-05-25
---

# Plan 27-02 — `docs/PERFORMANCE.md` per-operation budget table (PROFILE-02)

## Objective

Produce `docs/PERFORMANCE.md`: the per-operation budget table operators use
to reason about latency. Each of the 6 hot paths (with sub-paths) gets:
target p99, measurement methodology, observed p99 from the v1.9.0 baseline,
OK/WARN/FAIL thresholds (110% / 125% of target per D-210, aligned with
Phase 28's TUNE-02 CLI exit codes).

## Context

@.planning/phases/27-profiling/27-CONTEXT.md  (D-210)
@.planning/REQUIREMENTS.md                   (PROFILE-02)
@.planning/PROJECT.md                        (existing performance constraints — budget source)
@.planning/perf-baseline-v1.9.0.json         (observed baseline from plan 27-01)

## Budget derivation table (Performance constraints from PROJECT.md → per-operation target)

| Operation                      | PROJECT.md constraint                         | Target p99   |
|--------------------------------|-----------------------------------------------|--------------|
| combat-turn-resolution         | Discord ack <3s (orchestrator chain only)     | 500 ms       |
| mcp-cache-roundtrip.l1-hit     | Cache must be fast vs network                 | 1 ms         |
| mcp-cache-roundtrip.l1-miss-l2-hit | Cache miss should still beat dm20         | 10 ms        |
| mcp-cache-roundtrip.l1-l2-miss | Bound by mocked dm20 RTT only                 | 50 ms        |
| smart-driver-oracle.smart-success | Inner LLM-call path (mocked); D-54 1500ms ceiling for real call | 100 ms (our code only) |
| smart-driver-oracle.smart-fallback-to-random | Parse fail + random fallback     | 5 ms         |
| smart-driver-oracle.cache-hit  | Per-round LRU cache (D-56)                    | 1 ms         |
| character-ingest-fast-path     | <6s end-to-end (incl real LLM); OUR-CODE only | 50 ms        |
| ingest-pipeline-ocr            | <6s end-to-end; mocked OCR isolates our code  | 100 ms       |
| riposte-click-handler          | Discord ack <3s; click-to-resolve happy path  | 200 ms       |

**Methodology column language:** standardized phrase: "100 iter
wall-clock p99 via `scripts/perf/profile_hot_paths.py`; hermetic mocks
(respx dm20, AsyncMock LLM/Discord); cProfile run separate (20 iter)
populates `cprofile_top_10`."

**OK/WARN/FAIL thresholds (D-210):**
- OK ≤ target × 1.10
- WARN > target × 1.10
- FAIL > target × 1.25
(matches Phase 28 TUNE-02 exit codes: 0/1/2.)

## Tasks

### Task 1 (auto) — Write `docs/PERFORMANCE.md`

**Files:**
- `docs/PERFORMANCE.md` (new)

**Sections:**
1. **Purpose** — what this doc covers, what it doesn't (real dm20 RTT, real
   LLM latency).
2. **Hot paths** — bullet list of the 6 named paths with sub-paths.
3. **Budget table** — Operation | Target p99 | Observed p99 (v1.9.0) |
   OK/WARN/FAIL | Methodology. Observed-p99 column populated from
   `.planning/perf-baseline-v1.9.0.json` values committed by plan 27-01.
4. **Thresholds** — OK/WARN/FAIL math + reference to Phase 28 TUNE-02 CLI.
5. **Operator-tunable network budgets** (D-207) — dm20 RTT and ShoeGPT LLM
   inference are out-of-scope for OUR-code budget; documented separately
   with operator guidance ("run dm20 + omlx on same host for ≤5 ms RTT;
   ShoeGPT inference budget tracked in dm20's own metrics").
6. **Re-running the profiler** — one-liner invocation, when to commit a new
   baseline (D-212 — manual, deliberate acceptance of a regression).
7. **References** — links to CONTEXT.md D-206..D-214 and to Phase 28
   TUNE-02.

**Verification:**
- File exists, valid Markdown.
- All 6 hot paths + their sub-paths present in the budget table.
- OK/WARN/FAIL math shown explicitly.

**Done when:** doc written, observed values populated from committed
baseline.

### Task 2 (auto) — Lint check

**Files:** (none)

**Behavior:**
- `ruff check .` clean.
- `pytest -q` zero regression.

**Done when:** All gates green.

## Success Criteria

- [ ] `docs/PERFORMANCE.md` exists with per-operation budget table.
- [ ] Each of the 6 hot paths (+ sub-paths) has: target p99, observed p99
      (v1.9.0), OK/WARN/FAIL thresholds, methodology.
- [ ] WARN(110%) / FAIL(125%) thresholds documented and aligned with Phase
      28 TUNE-02 exit-code model.
- [ ] PROFILE-02 ticked [x] in REQUIREMENTS.md.

## Out of Scope

- Building the regression-detection CLI (Phase 28 TUNE-02).
- Dashboard / Phoenix integration of these budgets.

## Threat Model

Doc-only change; no code, no surface impact.
