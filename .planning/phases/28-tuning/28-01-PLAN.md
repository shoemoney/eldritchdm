---
phase: 28-tuning
plan: 28-01
milestone: v1.9
type: auto
autonomous: true
requirements: [TUNE-01]
depends_on: [27-01, 27-02]
---

# Plan 28-01 — TUNE-01: top-3 optimizations OR documented no-targets closure

## Objective

Apply D-216's empirical bar to the Phase 27 baseline. If any of the 10 baselined
operations have **material headroom against their per-op target budget** (≥10%
p99 reduction achievable OR moves out of WARN/FAIL), implement the top-3
optimizations with before/after evidence + regression-guard tests. Otherwise,
ship Branch B "no targets — codebase already within budget" closure (mirrors
Phase 25 CONC-03).

## Context

@.planning/phases/28-tuning/28-CONTEXT.md
@.planning/perf-baseline-v1.9.0.json
@docs/PERFORMANCE.md

## Analysis: Branch A vs Branch B (D-215, D-216)

Per-op budget arithmetic against the Phase 27 baseline + docs/PERFORMANCE.md
budget table:

| Operation | Target p99 | Observed p99 | % of budget | WARN/FAIL? |
|---|---:|---:|---:|---|
| riposte-click-handler                       | 200 ms | 3.573 ms | 1.79% | NO |
| ingest-pipeline-ocr                         | 100 ms | 1.446 ms | 1.45% | NO |
| character-ingest-fast-path                  |  50 ms | 1.084 ms | 2.17% | NO |
| combat-turn-resolution                      | 500 ms | 0.606 ms | 0.12% | NO |
| mcp-cache-roundtrip.l1-l2-miss              |  50 ms | 0.332 ms | 0.66% | NO |
| smart-driver-oracle.smart-fallback-to-random|   5 ms | 0.130 ms | 2.60% | NO |
| smart-driver-oracle.smart-success           | 100 ms | 0.118 ms | 0.12% | NO |
| mcp-cache-roundtrip.l1-miss-l2-hit          |  10 ms | 0.085 ms | 0.85% | NO |
| mcp-cache-roundtrip.l1-hit                  |   1 ms | 0.020 ms | 2.04% | NO |
| smart-driver-oracle.cache-hit               |   1 ms | 0.014 ms | 1.41% | NO |

**Result: Branch B applies.** Every operation is ≥45× under its target. None
are in WARN or FAIL. There is no operation where a 10% p99 reduction would
deliver user-observable benefit — Discord ack budget is 3s, character ingest
budget is 6s, narration budget is 1500ms. Even our slowest operation
(riposte-click at 3.57ms) is 0.12% of its Discord-ack budget.

Per D-215, manufacturing optimization work to avoid acknowledging that the
codebase is already fast violates the honesty clause. Ship Branch B.

## Tasks

<task id="1" type="auto">
  <name>Document Branch B no-targets closure</name>
  <files>
    - docs/PERFORMANCE.md (append §Phase 28 closure)
  </files>
  <behavior>
    Append a "Phase 28 TUNE-01 closure" section to docs/PERFORMANCE.md citing
    the per-op budget table above, the D-215 honesty clause, and the empirical
    bar from D-216. Cross-reference Phase 25 CONC-03 as precedent for a Branch B
    closure when profile-driven analysis finds no targets.
  </behavior>
  <verification>
    `grep -q "Phase 28 TUNE-01 closure" docs/PERFORMANCE.md`
  </verification>
</task>

<task id="2" type="auto">
  <name>Tick TUNE-01 in REQUIREMENTS.md</name>
  <files>
    - .planning/REQUIREMENTS.md
  </files>
  <behavior>
    Mark TUNE-01 as `[x]` with a brief note that it shipped as a Branch B
    no-targets closure with evidence in docs/PERFORMANCE.md.
  </behavior>
  <verification>
    `grep -q "\[x\] \*\*TUNE-01\*\*" .planning/REQUIREMENTS.md`
  </verification>
</task>

## Success Criteria

- [ ] docs/PERFORMANCE.md contains Phase 28 closure with per-op budget arithmetic
- [ ] TUNE-01 marked `[x]` in REQUIREMENTS.md
- [ ] No code changes (Branch B = documentation-only closure)
- [ ] Existing test suite still passes (no regressions introduced)
- [ ] ruff + lint-imports clean

## Output

- 28-01-SUMMARY.md documenting Branch B decision + evidence
- 28-01-VERIFICATION.md confirming success criteria
