---
phase: 24-ci-and-dashboards
plan: 24-02
type: standard
autonomous: true
wave: 1
depends_on: [24-01]
requirements: [POLISH-02, POLISH-03]
---

# Plan 24-02 — Cache dashboards + atomicity doc-fix + UPSTREAM-ISSUES.md (POLISH-02, POLISH-03)

## Objective

Bundle 3 Phoenix cache dashboards (mcp_cache, character_cache, narrcache) using the
Phase 11 OUR-FORMAT JSON spec. Extend `seed-dashboards.sh` (already iterates `*.json`,
so adding files is sufficient — verify idempotency). Reconcile the v1.6 atomicity
doc wording with shipped Phase 22 behavior. Create `.planning/UPSTREAM-ISSUES.md`
backlog file with the gsd-tools planner-template gap as first entry.

## Context

@.planning/phases/24-ci-and-dashboards/24-CONTEXT.md (D-188, D-189, D-190, D-191)
@database/dashboards/cache.json (Phase 11 OUR-FORMAT reference)
@scripts/observability/seed-dashboards.sh
@src/eldritch_dm/observability/instrumentation.py (span names: eldritch.mcp.cache,
  eldritch.mcp.cache.invalidation, eldritch.character_cache.lookup,
  eldritch.character_cache.invalidation, eldritch.narrcache.call)
@.planning/milestones/v1.6-REQUIREMENTS.md (line 64 — actual atomicity wording location)

## Deviation Note (pre-execution)

CONTEXT.md D-190 + REQUIREMENTS.md POLISH-03 both reference
`milestones/v1.5-REQUIREMENTS.md line 62` as the atomicity-wording target. The actual
text "atomic — partial wipes are forbidden" lives at `milestones/v1.6-REQUIREMENTS.md`
line 64 (OPQOL-03 is a v1.6 phase). Confirmed by grep across `.planning/`; v1.5 has
no atomic wording. v1.6-MILESTONE-AUDIT.md and Phase 22 SUMMARY also misreference
v1.5/line 62. Applying Rule 1 (auto-fix): edit the actual offending file
`v1.6-REQUIREMENTS.md`. Documented in 24-02-SUMMARY.md.

## Tasks

### Task 1 (auto) — Create `database/dashboards/mcp_cache.json`

**Files:**
- `database/dashboards/mcp_cache.json` (new)

**Implementation:** OUR-FORMAT spec (matches `cache.json` shape) with 3 panel-style
metrics: hit_rate, size, invalidations_total. Query recipe references span
`eldritch.mcp.cache` (attribute `eldritch.mcp.cache.layer`: l1_hit / l1_miss /
l2_hit / l2_miss) and `eldritch.mcp.cache.invalidation` (attribute
`eldritch.mcp.cache.invalidation.entries_removed`).

**Done when:** file parses as valid JSON; `phoenix_project_name` is `eldritch-mcp-cache`.

### Task 2 (auto) — Create `database/dashboards/character_cache.json`

**Files:**
- `database/dashboards/character_cache.json` (new)

**Implementation:** OUR-FORMAT spec. Span `eldritch.character_cache.lookup`
(attribute `eldritch.character_cache.layer`: hits_ttl / hits_etag / misses;
size attr `eldritch.character_cache.size`) and
`eldritch.character_cache.invalidation` (entries_removed).

**Done when:** valid JSON; `phoenix_project_name` is `eldritch-character-cache`.

### Task 3 (auto) — Create `database/dashboards/narrcache.json`

**Files:**
- `database/dashboards/narrcache.json` (new)

**Implementation:** OUR-FORMAT spec. Span `eldritch.narrcache.call`
(attribute `eldritch.narrcache.layer`: hit / miss / bypass; size attr
`eldritch.narrcache.size`; savings attr `eldritch.narrcache.savings_usd`).

**Done when:** valid JSON; `phoenix_project_name` is `eldritch-narrcache`.

### Task 4 (auto) — Verify `seed-dashboards.sh` idempotency

**Files:** none modified (script already iterates `*.json`).

**Implementation:** Manually trace: script globs `$DASHBOARDS_DIR/*.json`, queries
existing Phoenix projects, skips when name matches. New JSONs use unique
`phoenix_project_name` values (`eldritch-mcp-cache`, `eldritch-character-cache`,
`eldritch-narrcache`) that don't collide with existing 3 (`eldritch-latency`,
`eldritch-fallback`, `eldritch-cache`).

**Done when:** Confirmed in summary that script picks up new files automatically;
no script change needed.

### Task 5 (auto) — Atomicity doc-fix in `v1.6-REQUIREMENTS.md`

**Files:**
- `.planning/milestones/v1.6-REQUIREMENTS.md` (edit line 64)

**Implementation:** Change
> Phase 16↔17 invalidation wire (OPQOL-03) is atomic — partial wipes are forbidden; either both layers clear or neither.

to:
> Phase 16↔17 invalidation wire (OPQOL-03) is best-effort — partial-wipe is acceptable when caches are independent: log `eldritch.cache.partial_wipe` and continue (D-171/172, Phase 22 implementation reality).

**Done when:** wording reflects shipped Phase 22 behavior.

### Task 6 (auto) — Create `.planning/UPSTREAM-ISSUES.md`

**Files:**
- `.planning/UPSTREAM-ISSUES.md` (new)

**Implementation:** Header + ISSUE-1 entry for gsd-tools planner-template gap:
SUMMARYs don't consistently emit `requirements_completed:` frontmatter (v1.3
audit gap; `scripts/ci/check_summary_frontmatter.sh` is our local mitigation).

**Done when:** file exists with ISSUE-1 fully documented (repro, evidence,
suggested fix).

## Success Criteria

1. 3 dashboard JSONs parse cleanly and follow OUR-FORMAT spec
2. Each dashboard JSON has 3 metric blocks (hit_rate, size, invalidations)
3. `seed-dashboards.sh` requires no modification (verified by trace)
4. v1.6-REQUIREMENTS.md atomicity wording matches shipped behavior
5. `.planning/UPSTREAM-ISSUES.md` exists with ISSUE-1
6. ruff + lint-imports + pytest still green
