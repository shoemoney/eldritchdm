"""TacticalJudge + JudgeVerdict (Phase 12 / D-71, D-73).

This module is filled in across T-12-01-03 (JudgeVerdict + validator) and
T-12-01-04 (TacticalJudge AsyncOpenAI wrapper). T-12-01-01 lands only a
placeholder so ``eldritch_dm.eval.__init__`` can import the names.
"""

from __future__ import annotations

# Placeholder symbols. Filled in by T-12-01-03 and T-12-01-04.
JudgeVerdict = None  # type: ignore[assignment]
TacticalJudge = None  # type: ignore[assignment]
